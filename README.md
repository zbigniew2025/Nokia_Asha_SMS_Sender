This is a small linux console tool that allows you sending SMS thorugh your phone connected with USB cable.

It has hardcoded device  /dev/ttyACM0  

It must be run as root

It can use contacts list.  Just edit the file kontakty.txt

It allows only sending messages . It can't read them.

It is intended to easily write on computer keyboard instead small touchscreen keys.
